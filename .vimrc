" 行番号表示
set nu

" タブ数
set tabstop=4

set scrolloff=5

" 入力されているテキストの最大幅　0で無効
set textwidth=0

" バックアップを作成しない
set nobackup

" vim外部で変更されたときに自動的に読み込み
set autoread

" スワップファイルを作成しない
set noswapfile

" バックスペースでインデントや改行を削除
set backspace=indent,eol,start

" ステータス行を常に表示
set laststatus=2

" カーソルの行列数を表示
set ruler

" すべてのモードでマウス有効
set mouse=a

" カラー表示
syntax enable

set background=dark

